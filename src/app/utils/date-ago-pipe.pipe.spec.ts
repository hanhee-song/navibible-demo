import { DateAgoPipePipe } from './date-ago-pipe.pipe';

describe('DateAgoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateAgoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
